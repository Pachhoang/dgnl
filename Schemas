from pydantic import BaseModel, Field

class DishUpdate(BaseModel):
    name: str = Field(..., min_length=2, max_length=100, description="Tên món ăn")
    price: float = Field(..., gt=0, description="Giá món ăn phải lớn hơn 0")
    category_id: int = Field(..., description="ID danh mục món ăn")
